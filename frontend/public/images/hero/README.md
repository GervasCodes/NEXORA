# Hero imagery — placeholder

`Home.jsx` references three images here (`placeholder-1.jpg`, `placeholder-2.jpg`,
`placeholder-3.jpg`) for the homepage hero photo collage, added in Phase 5
(Visual Polish & Metadata). No real photography was supplied for this task,
so the files don't exist yet — the `<img>` tags fail gracefully (the
`onError` handler hides a broken image icon) and the hero falls back to its
gradient background until real assets are dropped in here.

Needed: three product/lifestyle photos representative of NEXORA's
marketplace — e.g. products, a seller preparing an order, a delivery
drop-off — matching the alt text already in `Home.jsx`.
